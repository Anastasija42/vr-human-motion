# Figure za izveštaj

Generisane (vektorske, spremne):
- `pipeline.pdf` — Slika 1: kompletan tok sistema (korisnik → Unity → server → Kimodo → reprodukcija).
- `demo_mapping.pdf` — Slika 2: VR demonstracija → Kimodo reprezentacija ograničenja.
- `retarget.pdf` — Slika 3: Kimodo skelet → Mecanim → IK korekcija kontakta.

(Regenerišu se skriptom `gen_report_figures.py` — matplotlib, sistemski python3.)

TODO (snimci ekrana, zakomentarisan `figure` blok u sekciji Scenario):
- `scene_demo.png` — zadavanje ograničenja u lab sceni (markeri dodira, plave
  tačke putanje, ljubičaste štap-figure poza).
- `scene_playback.png` — generisani karakter izvodi zadatak pored Panda robota.
